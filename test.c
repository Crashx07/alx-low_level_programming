#include <stdio.h>
/**
 * main - test if code snippets work
 * Return: 0 (depending on the code snippet)
 */
int main(void)
{
	int a, b;

	a = 0;
	for (a = 0; a < i; a++)
	{
		printf("%d is the numer?" a)
	}
}
