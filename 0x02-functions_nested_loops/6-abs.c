#include "main.h"
#include <stdlib.h>
/**
 * _abs - calculates and prints out the absolute value of an int
 * @i: interger input
 * Return: absolut value of a
 */
int _abs(int a)
{
	int b;

	b = abs(a);
	return b;
}
